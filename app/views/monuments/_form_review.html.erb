<div class="container">
  <div class="form">
    <h3>Add a review</h3>
<%= simple_form_for [ @monument, @review ] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
</div>
</div>
