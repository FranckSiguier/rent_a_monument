<div class="container">
  <h1><%= @monument.name %></h1>
  <% if @monument.photos.attached? %>
  <% @monument.photos.each do |photo| %>
    <%= cl_image_tag photo.key, height: 150, width: 200, crop: :fill , class: "text-center"%>
    <% end %>
  <% end %>
  <p><%= @monument.description %></p>
</div>

<%= render 'footer' %>
